apiVersion: 1
providers:
  - name: 'Default'
    orgId: 1
    folder: 'Network'
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      recursively: true

